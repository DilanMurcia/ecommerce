# Astro exposes vars prefixed with PUBLIC_ to the browser via import.meta.env.PUBLIC_*
# Copy this file to .env and adjust if your API runs elsewhere (e.g. production).
PUBLIC_API_URL=http://localhost:8080
